# aniplay

Простой скрипт на Python, который создаёт `.m3u` плейлист из папки с видеофайлами (аниме, сериалы), **сортируя их по номеру серии** — даже если имена вида `_[02]_`, `_[10]_`.

Работает «из коробки»: никаких зависимостей, только стандартная библиотека Python.

---

## 🔧 Что умеет

- Открывает системный проводник для выбора папки (мышкой — никакого ввода с клавиатуры)
- Ищет видеофайлы: `.mkv`, `.mp4`, `.avi`, `.webm`, `.mov`, `.flv`, `.wmv`
- Сортирует **естественным образом** (natural sort):  
  `[1]`, `[2]`, ..., `[9]`, `[10]`, `[11]` — ✅  
  (а не `[1]`, `[10]`, `[11]`, `[2]` — ❌)
- Создаёт `playlist.m3u` в той же папке с поддержкой `#EXTINF` (имена серий отображаются в VLC)
- Предлагает запустить плейлист в **VLC** (ищет `vlc.exe` автоматически)
- Работает с любыми именами папок и файлов: пробелы, `[ ]`, `—`, кириллица, `AniLibria`, `WEBRip` и т.п.

---

## 🚀 Как использовать

### Требования
- Python 3.6+ (входит в поставку Windows 10/11, или установить с [python.org](https://python.org))
- (Опционально) VLC — для автоматического запуска

### Запуск
1. Сохраните скрипт как `aniplay.py`
2. Дважды кликните по нему **или** запустите в терминале:
   ```bash
   python aniplay.py
3. В открывшемся проводнике выберите папку с аниме
4. Подтвердите создание плейлиста и запуск в VLC (если нужно)
Готово! Файл playlist.m3u появится в выбранной папке.

### 💡 Советы
Чтобы плейлисты всегда открывались в VLC:
ПКМ по playlist.m3u → Открыть с помощью → VLC → ✅ Всегда использовать этот вариант.
Скрипт не изменяет исходные файлы — только создаёт playlist.m3u.
Если VLC не установлен — плейлист можно открыть в любом плеере, поддерживающем .m3u.
